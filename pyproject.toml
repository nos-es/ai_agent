[project]
name = "ai-agent"
version = "0.1.0"
description = "This is a learning project focused on understanding how AI agents can use tools to inspect and work with code."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "google-genai==1.12.1",
    "python-dotenv==1.1.0",
]
